import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  absoluteCenter: {
    width: '100%',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    position: 'absolute',
  },
});
